from setuptools import setup, find_packages

setup(
    name="resume_automation",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List your project's dependencies here
        # They will be installed by uv using requirements.txt
    ],
)
